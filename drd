clear
./bin/www &
NPID=$!
NEXIT=$?
if [ ${NEXIT} -eq 0 ] ; then
	echo Running node server [PID: $NPID] 
	dredd apiary.apib http://localhost:3000
	kill $NPID
else
	echo Error running node server: $NEXIT
	kill $NPID
	exit 1
fi
